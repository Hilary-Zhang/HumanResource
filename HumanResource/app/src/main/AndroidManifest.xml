<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hilary.humanresource">

    <!-- 允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 获取GSM（2g）、WCDMA（联通3g）等网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- SD卡读取权限，用户写入离线定位数据 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 允许读取手机状态 用于创建BmobInstallation -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 获取sd卡写的权限，用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity android:name=".XinxiActivity" />
        <activity android:name=".ZhiduActivity" />
        <activity android:name=".GonggaoActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".QiandaoActivity" />
        <activity android:name=".QingjiaActivity" />
        <activity android:name=".ChuchaiActivity" />
        <activity android:name=".JiabanActivity" />
        <activity android:name=".LizhiActivity" />
        <activity android:name=".HetongActivity" />
        <activity android:name=".RizhiActivity" />
        <activity android:name=".XiangmuActivity" />
        <activity android:name=".XinziActivity" />
        <activity android:name=".WenjuanActivity" />
        <activity android:name=".JixiaoActivity" />
        <activity android:name=".PeixunActivity" />
        <activity android:name=".LiuyanActivity" />

        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote" />

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="D9S0n2ovNWXOlkDHhAEKDzr69ddN0UeE" />

        <activity android:name=".BanbenActivity" />
        <activity android:name=".GongnengActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".FankuiActivity" />
        <activity android:name=".GonggaoXiangqingActivity" />
        <activity android:name=".QingjiaListActivity"></activity>
    </application>

</manifest>